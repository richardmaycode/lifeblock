<%= render FormComponent.new do |f| %>
  <% f.header(title: "Create Reflection") %>
  <%= f.body(frame_id: "reflection_form") do |b| %>
    <%= render partial: "form", locals: { reflection: @reflection, current_user: @current_user } %>
  <% end %>
<% end %>